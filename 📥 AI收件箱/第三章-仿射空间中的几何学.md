---
title: "第三章 仿射空间中的几何学"
date: 2026-03-21
source: MinerU
subject: 其他
tags: [AI生成, MinerU, 其他]
---

# 第三章 仿射空间中的几何学

> 📥 由 AI 根据你的MinerU输入自动生成 · 2026-03-21
> 🔄 每晚23:59自动整理并归档到正式目录

```yaml
title: 第三章 仿射空间中的几何学
subject: 其他
source_path: /
---

# 3.1. 直线、平面、坐标与坐标变换

## 仿射空间
设 $\mathcal{A}^{n}$ 为非空集合。若有映射：
$$
\mathcal{A}^{n} \times \mathcal{A}^{n} \mapsto \mathbb{R}^{n}, \quad (A, B) \mapsto \overrightarrow{AB}
$$
满足：
1. $\forall v \in \mathbb{R}^{n}, A \in \mathcal{A}^{n}$，存在 $B \in \mathcal{A}^{n}$ 使得 $\overrightarrow{AB} = v$。
2. $\overrightarrow{AB} + \overrightarrow{BC} = \overrightarrow{AC}$（对任意 $A, B, C \in \mathcal{A}^{n}$）。

则称 $\mathcal{A}^{n}$ 为 $n$ 维仿射空间。

## 直线与平面
设 $\mathcal{A}^{3}$ 为三维仿射空间。
*   **直线**：$\mathcal{A}^{3}$ 的非空子集 $l$，若存在点 $A \in l$ 和非零向量 $v \in \mathbb{R}^{3}$，使得
    $$
    l = \{ B \in \mathcal{A}^{3} \mid \overrightarrow{AB} = k v, \, k \in \mathbb{R} \},
    $$
    则称 $l$ 为过点 $A$ 以 $v$ 为方向的直线。
*   **平面**：$\mathcal{A}^{3}$ 的非空子集 $\pi$，若存在点 $A \in \pi$ 和两个线性无关的向量 $v, w \in \mathbb{R}^{3}$，使得
    $$
    \pi = \{ B \in \mathcal{A}^{3} \mid \overrightarrow{AB} = k v + p w, \, (k, p) \in \mathbb{R}^{2} \},
    $$
    则称 $\pi$ 为过点 $A$ 并由 $v, w$ 张成的平面。

## 仿射坐标系
在 $\mathcal{A}^{n}$ 上选定一点 $O$（称为原点），并在 $\mathbb{R}^{n}$ 上选定一组基 $\{e_i\}$，则称 $\mathcal{A} = \{O, e_i\}$ 为 $\mathcal{A}^{n}$ 上的一个仿射坐标系。存在双射 $\varphi_{\mathcal{A}}$：
$$
\varphi_{\mathcal{A}}: \mathcal{A}^{n} \rightarrow \mathbb{R}^{n}, \quad A \longmapsto (x^{1}, \dots, x^{n}),
$$
其中 $\overrightarrow{OA} = \sum_{i=1}^{n} x^{i} e_i$。$(x^{1}, \dots, x^{n})$ 称为点 $A$ 在坐标系 $\mathcal{A}$ 下的坐标。

## 仿射坐标变换
考虑两个仿射坐标系 $\mathcal{A} = \{O, e_i\}$ 和 $\mathcal{A}' = \{O', f_i\}$。它们之间的关系由下式给出：
$$
\overrightarrow{OO'} = \sum_{i=1}^{n} a^{i} e_i, \quad e_i = \sum_{j=1}^{n} T_i^{\, j} f_j.
$$
用矩阵形式表示为：
$$
(e_1, e_2, \dots, e_n) = (f_1, f_2, \dots, f_n) \begin{pmatrix}
T_1^{\, 1} & T_2^{\, 1} & \dots & T_n^{\, 1} \\
T_1^{\, 2} & T_2^{\, 2} & \dots & T_n^{\, 2} \\
\vdots & \vdots & & \vdots \\
T_1^{\, n} & T_2^{\, n} & \dots & T_n^{\, n}
\end{pmatrix}.
$$

**命题**：设点 $A$ 在坐标系 $\mathcal{A}$ 下的坐标为 $(x^{1}, \dots, x^{n})$，在坐标系 $\mathcal{A}'$ 下的坐标为 $(y^{1}, \dots, y^{n})$，则它们之间的关系为：
$$
y^{i} = \sum_{j=1}^{n} T_j^{\, i} (x^{j} - a^{j}), \quad \text{或} \quad \begin{pmatrix} y^{1} \\ y^{2} \\ \vdots \\ y^{n} \end{pmatrix} = \begin{pmatrix}
T_1^{\, 1} & T_2^{\, 1} & \dots & T_n^{\, 1} \\
T_1^{\, 2} & T_2^{\, 2} & \dots & T_n^{\, 2} \\
\vdots & \vdots & & \vdots \\
T_1^{\, n} & T_2^{\, n} & \dots & T_n^{\, n}
\end{pmatrix} \begin{pmatrix} x^{1} - a^{1} \\ x^{2} - a^{2} \\ \vdots \\ x^{n} - a^{n} \end{pmatrix}.
$$

**证明**：
$$
\begin{aligned}
\overrightarrow{O'A} &= \overrightarrow{OA} - \overrightarrow{OO'} = \sum_{i=1}^{n} x^{i} e_i - \sum_{i=1}^{n} a^{i} e_i = \sum_{i=1}^{n} (x^{i} - a^{i}) e_i \\
&= \sum_{i=1}^{n} (x^{i} - a^{i}) \sum_{j=1}^{n} T_i^{\, j} f_j = \sum_{j=1}^{n} \left[ \sum_{i=1}^{n} T_i^{\, j} (x^{i} - a^{i}) \right] f_j.
\end{aligned}
$$
又因为 $\overrightarrow{O'A} = \sum_{j=1}^{n} y^{j} f_j$，比较分量即得 $y^{j} = \sum_{i=1}^{n} T_i^{\, j} (x^{i} - a^{i})$。

*   当变换矩阵的行列式 $\det(T_i^{\, j}) > 0$ 时，称两个坐标系**同向**；反之称为**反向**。

# 3.2. 几何量
几何量是仅依赖于几何对象本身，而不依赖于坐标系选取的量。

# 3.3. 拓扑与实标量场（函数）

## 拓扑
设 $\mathcal{A} = \{O, e_i\}$ 是 $\mathcal{A}^{n}$ 的一个仿射坐标系。$\mathcal{A}^{n}$ 的子集 $U$ 称为**开集**，如果它在坐标映射 $\varphi_{\mathcal{A}}$ 下的像 $\varphi_{\mathcal{A}}(U)$ 是 $\mathbb{R}^{n}$ 中的开集。

**引理 3.1**：一个子集 $B \subset \mathcal{A}^{n}$ 在某个仿射坐标系下是开集，当且仅当它在任意仿射坐标系下都是开集。

**证明概要**：设 $B$ 在坐标系 $\mathcal{A}$ 下是开集。考虑坐标变换映射 $\varphi_{\mathcal{A}'} \circ \varphi_{\mathcal{A}}^{-1}: \mathbb{R}^{n} \to \mathbb{R}^{n}$。这是一个仿射变换（由可逆矩阵 $T$ 和平移向量决定），因而是 $\mathbb{R}^{n}$ 到自身的同胚映射。由于同胚映射保持开集性质，且 $\varphi_{\mathcal{A}'}(B) = (\varphi_{\mathcal{A}'} \circ \varphi_{\mathcal{A}}^{-1})(\varphi_{\mathcal{A}}(B))$，而 $\varphi_{\mathcal{A}}(B)$ 是开集，故 $\varphi_{\mathcal{A}'}(B)$ 也是开集。反之亦然。

## 函数/标量场
设 $U$ 是 $\mathcal{A}^{n}$ 的一个开区域（连通开集），$f: U \to \mathbb{R}$ 是一个函数。给定一个仿射坐标系 $\mathcal{A} = \{O, e_i\}$，我们可以通过 $f$ 定义一个 $\mathbb{R}^{n}$ 上的 $n$ 元函数 $f_{\mathcal{A}}$：
$$
f_{\mathcal{A}} = f \circ \varphi_{\mathcal{A}}^{-1}: \varphi_{\mathcal{A}}(U) \subset \mathbb{R}^{n} \to \mathbb{R}.
$$
称 $f_{\mathcal{A}}$ 为从坐标系 $\mathcal{A}$ 中**读取** $f$ 得到的函数。

**引理 3.2**：设 $U$ 为 $\mathcal{A}^{n}$ 中开区域，$f$ 为 $U$ 上函数，则以下三点等价：
1.  $f$ 是连续的（按 $\mathcal{A}^{n}$ 上由仿射坐标系诱导的拓扑）。
2.  在某个仿射坐标系 $\mathcal{A}$ 中读取 $f$ 得到的 $n$ 元函数 $f_{\mathcal{A}}$ 连续。
3.  在任意仿射坐标系中读取 $f$ 得到的 $n$ 元函数均连续。

**证明思路**：利用坐标变换映射 $\varphi_{\mathcal{A}'} \circ \varphi_{\mathcal{A}}^{-1}$ 是同胚（连续且逆连续）这一事实，以及连续函数的复合保持连续性。

## 可微性
设 $U$ 是 $\mathcal{A}^{n}$ 上开区域，$f$ 为 $U$ 上函数。称 $f$ **可微**，如果存在某个仿射坐标系 $\mathcal{A}$，使得读取得到的函数 $f_{\mathcal{A}}$ 是可微的。

**引理 3.3**：设 $U$ 是 $\mathcal{A}^{n}$ 上开区域，$f$ 为 $U$ 上函数。若 $f$ 在某个仿射坐标系下的读取可微，则在所有仿射坐标系下的读取均可微。

**证明**：设 $f$ 在坐标系 $\mathcal{A}$ 下读取可微，即 $f \circ \varphi_{\mathcal{A}}^{-1}$ 可微。对于另一坐标系 $\mathcal{A}'$，有 $f \circ \varphi_{\mathcal{A}'}^{-1} = (f \circ \varphi_{\mathcal{A}}^{-1}) \circ (\varphi_{\mathcal{A}} \circ \varphi_{\mathcal{A}'}^{-1})$。由于 $\varphi_{\mathcal{A}} \circ \varphi_{\mathcal{A}'}^{-1}$ 是 $\mathbb{R}^{n}$ 区域间的光滑（$C^{\infty}$）同胚（仿射变换），而可微函数与光滑函数的复合仍可微，故 $f \circ \varphi_{\mathcal{A}'}^{-1}$ 可微。

# 3.4. 广义坐标系、广义坐标变换

## 广义坐标系
设 $U$ 是 $\mathcal{A}^{n}$ 上的一个开区域，$\{y^{i}\}$ 是定义在 $U$ 上的一族 $C^{\infty}$ 函数。定义映射：
$$
\varphi_{U}: U \to \mathbb{R}^{n}, \quad A \mapsto (y^{1}(A), \dots, y^{n}(A)).
$$
固定 $\mathcal{A}^{n}$ 上的一个仿射坐标系 $\mathcal{A} = \{O, e_i\}$，其坐标映射为 $\varphi_{\mathcal{A}}$，点的坐标记为 $\{x^{i}\}$。

如果满足：
1.  $\varphi_{U}: U \to \varphi_{U}(U)$ 是双射。
2.  复合映射 $\varphi_{U} \circ \varphi_{\mathcal{A}}^{-1}: \varphi_{\mathcal{A}}(U) \to \varphi_{U}(U)$ 和 $\varphi_{\mathcal{A}} \circ \varphi_{U}^{-1}: \varphi_{U}(U) \to \varphi_{\mathcal{A}}(U)$ 都是 $C^{\infty}$ 映射（光滑映射）。

则称 $\{U, y^{i}\}$（或简记为 $\{y^{i}\}$）为定义在 $U$ 上的一个**广义坐标系**（或**曲线坐标系**）。

**命题 3.2**：设 $U$ 为 $\mathcal{A}^{n}$ 中开区域，带有广义坐标系 $\{U, y^{i}\}$，则：
1.  $U$ 中的开子集在 $\varphi_{U}$ 下的像是 $\mathbb{R}^{n}$ 中的开子集；反之，$\mathbb{R}^{n}$ 中的开子集在 $\varphi_{U}^{-1}$ 下的原像是 $U$ 中的开子集。
2.  $f$ 为 $U$ 上定义的标量场，则 $f$ 连续 $\Leftrightarrow$ $f \circ \varphi_{U}^{-1}$ 是 $\varphi_{U}(U)$ 上的连续函数。

**命题 3.3**：设 $U$ 为 $\mathcal{A}^{n}$ 上开区域，$f$ 为 $U$ 上函数，则下列陈述等价：
1.  $f$ 在某个仿射坐标系中读取可微（即 $f$ 可微）。
2.  $f$ 在某个广义坐标系下读取可微。
3.  $f$ 在任意广义坐标系下读取可微。

**证明思路**：利用广义坐标系与仿射坐标系之间的转换映射 $\varphi_{\mathcal{A}} \circ \varphi_{U}^{-1}$ 及其逆是 $C^{\infty}$ 的这一性质，通过复合来证明等价性。例如，(1) $\Rightarrow$ (2)：$f \circ \varphi_{U}^{-1} = (f \circ \varphi_{\mathcal{A}}^{-1}) \circ (\varphi_{\mathcal{A}} \circ \varphi_{U}^{-1})$，右边是可微函数与光滑函数的复合，故可微。

# 3.5. 向量值函数与向量场

## 切空间
设 $A \in \mathcal{A}^{n}$，定义 $A$ 点的**切空间**为：
$$
T_{A}\mathcal{A}^{n} = \{(A, B) \mid B \in \mathcal{A}^{n} \}.
$$
在 $T_{A}\mathcal{A}^{n}$ 上定义加法和数乘：
*   **加法**：$(A, B) + (A, C) = (A, D)$，其中 $\overrightarrow{AD} = \overrightarrow{AB} + \overrightarrow{AC}$。
*   **数乘**：$\lambda (A, B) = (A, C)$，其中 $\overrightarrow{AC} = \lambda \overrightarrow{AB}$。

在此运算下，$T_{A}\mathcal{A}^{n}$ 成为一个 $n$ 维实向量空间，且与 $\mathbb{R}^{n}$ 同构。元素 $(A, B)$ 常简记为 $\overrightarrow{AB}$，并可通过**平行移动**与 $\mathbb{R}^{n}$ 中的向量等同。

给定仿射坐标系 $\mathcal{A} = \{O, e_i\}$，向量 $\overrightarrow{AB} \in T_{A}\mathcal{A}^{n}$ 可以表示为 $\overrightarrow{AB} = \sum_{i=1}^{n} X^{i} e_i$，$(X^{1}, \dots, X^{n})$ 称为其**分量形式**。

## 向量场
设 $U \subset \mathcal{A}^{n}$ 为一个区域。$U$ 上的一个**向量场** $X$ 是一个映射，为 $U$ 中每一点 $A$ 指定一个该点的切向量：
$$
X: U \to \bigcup_{A \in U} T_{A}\mathcal{A}^{n}, \quad A \mapsto X(A) \in T_{A}\mathcal{A}^{n}.
$$

在仿射坐标系 $\mathcal{A} = \{O, e_i\}$ 下，向量场 $X$ 在点 $A$ 的分量可以写为 $X(A) = \sum_{i=1}^{n} X^{i}(A) e_i$。这给出了 $n$ 个定义在 $U$ 上的函数 $(X^{1}(A), \dots, X^{n}(A))$，称为向量场 $X$ 在坐标系 $\mathcal{A}$ 下的**分量形式**，即映射：
$$
U \to \mathbb{R}^{n}, \quad A \mapsto (X^{1}(A), \dots, X^{n}(A)).
$$
```